%h2 Words view
%p
  You have
  = @words.count
  words in database!
%table.table
  %thead
    %tr
      %th
        \#
      %th
        Content
      %th
        Language 
      %th
        Actions
        %tbody
        - @words.each do |word|
          %tr.table-active
            %td
              = word.id
            %td
              = word.content
            %td
              = word.language
            %td
              = link_to('Show', word_path(word), class: 'btn btn-primary')