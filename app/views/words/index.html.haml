%h2 Words view
%p
  You have
  = @words.count
  words in database!
%table.table
  %thead
    %tr
      %th
        \#
      %th
        Content
      %th
        Language 
      %th
        User 
      %th
        Actions
        %tbody
        - @words.each do |word|
          %tr.table-active
            %td
              = word.id
            %td
              = word.content
            %td
              = word.language&.name
            %td
              = word.user&.email
            %td
              = link_to('Show', word_path(word), class: 'btn btn-primary')
              - if word.user == current_user
                = link_to('Edit', edit_word_path(word), class: 'btn btn-primary')
                = link_to('Delete', word_path(word), class: 'btn btn-danger', method: :delete)